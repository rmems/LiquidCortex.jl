name: TagBot

on:
  issue_comment:
    types:
      - created
  workflow_dispatch:

concurrency:
  group: tagbot-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  issues: read

jobs:
  TagBot:
    if: github.event_name == 'workflow_dispatch' || github.actor == 'JuliaTagBot'
    runs-on: ubuntu-latest
    steps:
      - uses: JuliaRegistries/TagBot@cded32665b34ca0496d3ac10a67d08ddfc726963 # v1.25.11
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # SSH so the tag push can trigger documentation.yml (GITHUB_TOKEN
          # events do not start new workflows). Same deploy key as Documenter.
          ssh: ${{ secrets.DOCUMENTER_KEY }}
