# Coverage flags:
# - `julia` — CPU-reachable slice (ubuntu-latest)
# - `julia-gpu` — reservoir dynamics from the self-hosted GPU runner
#
# See issue #55. Project and patch statuses stay informational until CPU-side
# core tests exist (#57) and GPU coverage has a stable baseline.
#
# Revisit when #55 is fully closed: drop `informational` and set real targets.

coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
